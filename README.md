# slo-watch
The SLA/SLO analyser: paste or upload a ticket CSV and it computes compliance, MTTR by category, recurring incidents and a day-by-hour staffing heatmap, all in plain JS; the model only writes the exec summary from those numbers. 20/20 smoke tests pass, 0 em dashes, distinct teal-green design.
